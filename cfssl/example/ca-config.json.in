{
  "signing": {
    "default": {
      "expiry": "${CFSSL_EXP}"
    },
    "profiles": {
      "kubernetes": {
        "usages": [
          "signing",
          "key encipherment",
          "server auth",
          "client auth"
        ],
        "expiry": "${CFSSL_EXP}"
      }
    }
  }
}
