{
  "CN": "${CFSSL_CN}",
  "hosts": [
    "127.0.0.1",
    "${MASTER_IP}",
    "${MASTER_CLUSTER_IP}",
    "kubernetes",
    "kubernetes.default",
    "kubernetes.default.svc",
    "kubernetes.default.svc.cluster",
    "kubernetes.default.svc.cluster.local"
  ],
  "key": {
    "algo": "${CFSSL_ALGO}",
    "size": ${CFSSL_SIZE}
  },
  "names": [{
    "C": "${CFSSL_COUNTRY}",
    "ST": "${CFSSL_STATE}",
    "L": "${CFSSL_CITY}",
    "O": "${CFSSL_ORG}",
    "OU": "${CFSSL_OU}"
  }]
}
