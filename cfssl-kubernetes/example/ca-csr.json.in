{
  "CN": "${CFSSL_CN}",
  "key": {
    "algo": "${CFSSL_ALGO}",
    "size": ${CFSSL_SIZE}
  },
  "names": [
    {
      "C": "${CFSSL_COUNTRY}",
      "ST": "${CFSSL_STATE}",
      "L": "${CFSSL_CITY}",
      "O": "${CFSSL_ORG}",
      "OU": "${CFSSL_OU}"
    }
  ]
}
